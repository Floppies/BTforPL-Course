typestate FileServerProtocol {
  Init = {
    boolean start(Socket): <true: Open, false: end>
  }
  Open = {
    boolean hasRequest(): <true: Send, false: Close>
  }
  Send = {
    void sendFile(String): Close
  }
  Close = {
     void close(): end
  }
}